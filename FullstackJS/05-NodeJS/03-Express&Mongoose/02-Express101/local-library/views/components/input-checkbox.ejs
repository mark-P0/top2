<%#
 % Dedicated component to allow Prettier in files where it would otherwise go
 % Because apparently the conditional `checked` pattern below is valid, but the formatter does not like it
-%>
<input
  type="checkbox"
  name="<%= name -%>"
  value="<%= value -%>"
  <% if (isChecked) { -%> checked <% } -%>
/>